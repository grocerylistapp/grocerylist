export interface ShoppingItem{
        $key?: string,
        itemName : string;
        itemNumber?: number;
        itemKey?: string;
        store?: string;
        status?: string;
        share?: Array<any>;
}